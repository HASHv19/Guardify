# External Datasets

Drop larger datasets here when available. The project already supports config-driven adapters for:

- HASOC-style CSV
- TRAC-style CSV
- Custom CSV
- Custom JSON

Keep these files out of source control if they contain sensitive or licensed data.

